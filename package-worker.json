{
  "name": "allorigins-worker",
  "description": "Pull contents from any page as JSON via API - Cloudflare Worker Version",
  "author": "Gabriel Nunes <gabriel@multiverso.me>",
  "version": "2.7.0",
  "type": "module",
  "main": "worker.js",
  "license": "MIT",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "jest --coverage ./src",
    "prettier": "prettier -c ./src"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240112.0",
    "@types/jest": "^29.0.3",
    "jest": "^29.0.3",
    "prettier": "^2.7.1",
    "wrangler": "^3.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}